# 🚀 PayGate Optimizer - Production Package
# باتش الإنتاج الكامل

---

## 📋 محتويات الباتش

```
production/
├── database/
│   ├── 001_create_tables.sql    # إنشاء الجداول
│   └── 002_seed_data.sql        # البيانات الأولية
├── env.txt                       # ملف البيئة (انسخه إلى .env)
├── server.js                     # خادم Node.js
├── setup-database.php            # سكريبت إعداد قاعدة البيانات
└── README.md                     # هذا الملف
```

---

## 🔧 خطوات الرفع (بالترتيب)

### الخطوة 1: رفع الملفات
1. ارفع **كل المشروع** إلى `public_html` أو مجلد الدومين
2. تأكد من رفع مجلد `.next` (مهم جداً!)
3. تأكد من رفع مجلد `production/database/`

### الخطوة 2: إعداد ملف البيئة
```bash
# انسخ محتوى production/env.txt إلى .env
# وغيّر كلمة المرور:
DATABASE_URL=mysql://optg_pay:YOUR_ACTUAL_PASSWORD@localhost:3306/optg_pay
```

### الخطوة 3: إنشاء قاعدة البيانات
**الطريقة 1 - عبر المتصفح:**
```
https://pay.op-tg.com/production/setup-database.php?key=PayGateSetup2025
```

**الطريقة 2 - عبر phpMyAdmin:**
1. افتح phpMyAdmin
2. اختر قاعدة البيانات `optg_pay`
3. اذهب إلى Import
4. ارفع `001_create_tables.sql` ثم `002_seed_data.sql`

### الخطوة 4: تثبيت الحزم وتشغيل التطبيق

**في cPanel Node.js App:**
1. اذهب إلى Setup Node.js App
2. أنشئ تطبيق جديد:
   - Node.js version: 18+
   - Application root: المجلد
   - Application URL: pay.op-tg.com
   - Startup file: server.js
3. اضغط Create

**في Terminal:**
```bash
npm install --production
```

### الخطوة 5: تشغيل التطبيق
اضغط **START APP** في cPanel

---

## 🔐 بيانات الدخول

| الحقل | القيمة |
|-------|--------|
| **البريد** | `admin@op-tg.com` |
| **كلمة المرور** | `PayGate@2025` |

---

## ⚠️ تنبيهات مهمة

1. **احذف** ملف `setup-database.php` بعد إعداد قاعدة البيانات
2. **غيّر** كلمة المرور بعد أول تسجيل دخول
3. **تأكد** من أن NEXTAUTH_SECRET فريد وآمن

---

## ✅ قائمة التحقق

- [ ] ملفات المشروع مرفوعة
- [ ] مجلد .next موجود
- [ ] ملف .env تم إنشاؤه وتعديله
- [ ] قاعدة البيانات تم إعدادها
- [ ] التطبيق يعمل
- [ ] تسجيل الدخول يعمل
- [ ] تم حذف setup-database.php

---

## 🔗 الروابط

| الصفحة | الرابط |
|--------|--------|
| الرئيسية | https://pay.op-tg.com |
| المزودين | https://pay.op-tg.com/providers |
| المقارنة | https://pay.op-tg.com/wizard |
| لوحة التحكم | https://pay.op-tg.com/admin |

